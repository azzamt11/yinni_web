version: '3.8'

services:
  yinni-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: main
    ports:
      - "3000:80" # Maps your computer's port 3000 to the container's port 80
    restart: always